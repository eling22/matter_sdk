# This is a basic workflow to help you get started with Actions

name: Build example - Linux 

on:
    pull_request:
    merge_group:

concurrency:
    group: ${{ github.ref }}-${{ github.workflow }}-${{ (github.event_name == 'pull_request' && github.event.number) || (github.event_name == 'workflow_dispatch' && github.run_number) || github.sha }}
    cancel-in-progress: true

env:
    CHIP_NO_LOG_TIMESTAMPS: true
    
jobs:
    linux_standalone:
        name: Linux Standalone

        runs-on: ubuntu-latest
        if: github.actor != 'restyled-io[bot]'

        container:
            image: connectedhomeip/chip-build:0.7.3
            volumes:
                - "/tmp/bloat_reports:/tmp/bloat_reports"

        steps:
            - name: Checkout
              uses: actions/checkout@v3

            - name: Checkout submodules & Bootstrap
              uses: ./.github/actions/checkout-submodules-and-bootstrap
              with:
                platform: linux

            - name: Set up environment for size reports
              uses: ./.github/actions/setup-size-reports
              if: ${{ !env.ACT }}
              with:
                gh-context: ${{ toJson(github) }}

            - name: Build lighting-app
              run: ./scripts/examples/gn_rt582_example.sh ./examples/lighting-app/rt582 ./out/lighting-app RT582

            - name: Uploading lighting-app
              uses: actions/upload-artifact@v3
              with:
                name: lighting-app_bin
                path: ./out/lighting-app/RT582/chip-rt582-lighting-app-example.bin

            - name: Report lighting-app size
              run: |
                  .environment/pigweed-venv/bin/python3 scripts/tools/memory/gh_sizes.py \
                    linux debug lighting-app \
                    out/lighting-app/RT582/chip-rt582-lighting-app-example.bin \
                    /tmp/bloat_reports/

            - name: Build light-switch-app
              run: ./scripts/examples/gn_rt582_example.sh ./examples/light-switch-app/rt582 ./out/light-switch-app RT582

            - name: Uploading light-switch-app
              uses: actions/upload-artifact@v3
              with:
                name: light-switch-app_bin
                path: ./out/light-switch-app/RT582/chip-rt582-light-switch-app-example.bin

            - name: Clean out build output
              run: rm -rf ./out

            - name: Uploading Size Reports
              uses: ./.github/actions/upload-size-reports
              if: ${{ !env.ACT }}
              with:
                platform-name: Linux-Standalone